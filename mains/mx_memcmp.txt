int main()
{
    unsigned char src[] = "12ek";
    unsigned char dst[] = "1334567890";

    // Сравниваем первые n байт массивов
    // и результат выводим на экран
    int w = mx_memcmp(src, dst, 15);
    printf("%d\n", w);
    return 0;
}
