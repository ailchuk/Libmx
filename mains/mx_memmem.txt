int main()
{
    char *str = "Hellobadgood";
    size_t s_len = 12;
    char *ntr = "ll";
    size_t n_len = 2;

//    printf("%s\n", mx_memmem(str, s_len, ntr, n_len));
    printf("%s\n", mx_memmem(str, s_len, ntr, n_len));
    printf("%s\n", memmem(str, s_len, ntr, n_len));
    system("leaks a.out");

}
